config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 1
      rampTo: 1
scenarios:
    - flow:
      - get: 
          url: "/video/C8ukMacEEJF"
      - post: 
          url: "/upload"
          json:
            body: "{"what":"the"}"