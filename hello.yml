config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 2
      rampTo: 1000
scenarios:
    - flow:
      - get: 
          url: "/video/C8ukMacEEJF"
      - post: 
          url: "/upload"
          json:
            body: "{what:'the'}"